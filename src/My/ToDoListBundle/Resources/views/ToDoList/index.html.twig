<h1>My To Do Items</h1>
<table class="table">
  <thead>
    <tr>
        <td>ID</td>
        <td>登録日</td>
        <td>タイトル</td>
        <td>締切日</td>
    </tr>
  </thead>
  <tbody>
    {# posts配列をループして、投稿記事の情報を表示 #}
    {% for post in posts %}
    <tr>
        <td>{{ post.id }}</td>
        <td>{{ post.createdAt|date('Y/m/d H:i') }}</td>
        <td><a href="">{{ post.title }}</a></td>
        <td><a href="">{{ post.deadline }}</a></td>
    </tr>
    {% else %}
    <tr>
        <td colspan="3">No Posts</td>
    </tr>
    {% endfor %}
  </tbody>
</table>
